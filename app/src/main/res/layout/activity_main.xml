<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.henry.myapplication.activity.MainActivity">

   <Button
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:onClick="showDialog"
       android:text="点我弹出自定义对话框"
       android:id="@+id/button" />

   <Button
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:onClick="showWheelPicker"
       android:id="@+id/showWheelPickerbtn"
       android:text="点我弹出滚动控件"
       android:layout_marginLeft="9dp"
       android:layout_marginStart="9dp"
       android:layout_marginTop="7dp"
       android:layout_below="@+id/button"
       android:layout_alignParentLeft="true"
       android:layout_alignParentStart="true" />

   <Button
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:id="@+id/btnDivergeView"
       android:text="点赞效果"
       android:onClick="showDivergeView"
       android:layout_marginLeft="11dp"
       android:layout_marginStart="11dp"
       android:layout_marginTop="8dp"
       android:layout_below="@+id/showWheelPickerbtn"
       android:layout_alignLeft="@+id/showWheelPickerbtn"
       android:layout_alignStart="@+id/showWheelPickerbtn" />

   <Button
       android:text="上拉刷新下拉加载"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:layout_below="@+id/btnDivergeView"
       android:layout_alignRight="@+id/btnDivergeView"
       android:layout_alignEnd="@+id/btnDivergeView"
       android:layout_marginTop="15dp"
       android:id="@+id/btn_pull_to_refresh_listview"
       android:onClick="showPullToRefresh" />


</RelativeLayout>
